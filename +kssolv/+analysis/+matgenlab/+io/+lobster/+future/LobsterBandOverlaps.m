classdef LobsterBandOverlaps
    %#ok<*MCSCT,*ALIGN,*AGROW,*ISCL,*MCNPN,*STOUT,*UNRCH,*MCCBU,*MSNU>
    %LOBSTERBANDOVERLAPS Typed value container for overlap data.
    properties
        k_points (1,1) struct = struct()
        matrices (1,1) struct = struct()
        max_deviations (1,1) struct = struct()
    end
end
